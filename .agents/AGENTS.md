# Project Rules

- Do not use markdown highlights (bolding `**text**`, italics `*text*`, or inline backticks `` `text` ``) in Markdown documentation or Jupyter notebook cells. Keep text clean and plain without formatting highlights.
